[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "matchbox"
version = "0.1.0"
requires-python = ">=3.6"
dependencies = [
    "pypdf",
    "striprtf",
]

[project.scripts]
matchbox = "src.__main__:main"
